Dictionaries are unordered mappings for storing objetcts and cannot be sorted.

Dictionaries use a key-value pairing. This key-value pair allows users to quickly grab objects without needing to know an index location.

Dictionaries use {} and colons to signify the keys and their associated values.

dictionary.keys() = brings back all the keys included in a dictionary
dictionary.values() = returns a list of the values sorted in that dictionary 
dictionary.items() = brings back all the pairs

dictionary[key] = value > to add or change values
del dictionary[key] > remove items from a dictionary 
value in dictionary.values() > to determine if a value exists
